name: Deploy Dev
on:
  push:
    branches:
      - main

jobs:
  call-deploy-bff:
    uses: ./.github/workflows/deploy.yml
    with:
      ENVIRONMENT: dev
      CERTIFICATE_ARN: arn:aws:acm:us-east-1:223889111609:certificate/073fac82-28de-464b-b55b-60f6dd6997dc
    secrets:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
